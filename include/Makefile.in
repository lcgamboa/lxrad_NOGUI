include ../Makefile.Conf

all:

static:

install:
	${MKDIR} ${includedir}/lxrad_NOGUI
	${CP} -v *.h ${includedir}/lxrad_NOGUI/
	chmod 644 ${includedir}/lxrad_NOGUI/*

uninstall:
	${RM} -R ${includedir}/lxrad_NOGUI

clean:
